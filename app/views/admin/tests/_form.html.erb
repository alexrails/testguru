<%= form_with model: [:admin, @test], local: true do |f| %>

  <%= render 'errors' %>

  <p>
    <%= f.label :title  %>
    <%= f.text_field :title, id: :test_title %>
  </p>

  <p>
    <%= f.label :level %>
    <%= f.number_field :level, id: :test_level %>
  </p>

  <p>
    <%= f.label :timer %>
    <%= f.number_field :timer, id: :timer %>
  </p>

  <p>
    <%= f.label :category_id %>
    <%= f.collection_select :category_id, Category.all, :id, :title, prompt: true %>
  </p>

  <p><%= f.submit %></p>
<% end %>

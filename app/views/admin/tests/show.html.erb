<h1>
  <%= t('.header1') %> - <%= @test.title %>
</h1>
<h3 class="text-muted">
  <%= t('.header2') %> <%= @test.level %>
</h3>
<%= link_to t('.new_question'), (new_admin_test_question_path @test), class: 'btn btn-light'  %>
<hr class="my-4">
<table class="table">
  <thead class="thead-dark">
    <tr class="text-center">
      <th><%= t('.question') %></th>
      <th colspan=3><%= t('.action') %></th>
    </tr>
  </thead>

  <%= render 'test_questions'%>

 </table>

